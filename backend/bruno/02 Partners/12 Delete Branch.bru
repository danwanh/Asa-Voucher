meta {
  name: 12 Delete Branch
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/branches/{{createdBranchId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{partnerToken}}
}

tests {
  test("delete branch soft disables it", function () {
    expect(res.status).to.equal(204);
  });
}
