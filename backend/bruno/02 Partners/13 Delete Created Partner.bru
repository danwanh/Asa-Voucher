meta {
  name: 13 Delete Created Partner
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/partners/{{createdPartnerId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{adminAccountToken}}
}

tests {
  test("admin closes partner", function () {
    expect(res.status).to.equal(204);
  });
}
