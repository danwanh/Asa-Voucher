meta {
  name: 10 Get Branch
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/branches/{{createdBranchId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{partnerToken}}
}

tests {
  test("get created branch", function () {
    expect(res.status).to.equal(200);
    expect(res.body.data.id).to.equal(bru.getVar("createdBranchId"));
  });
}
