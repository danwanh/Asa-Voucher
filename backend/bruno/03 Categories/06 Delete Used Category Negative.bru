meta {
  name: 06 Delete Used Category Negative
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/categories/{{categoryId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{adminContentToken}}
}

tests {
  test("cannot delete category used by voucher", function () {
    expect(res.status).to.equal(409);
  });
}
