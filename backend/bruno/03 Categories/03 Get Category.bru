meta {
  name: 03 Get Category
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/categories/{{createdCategoryId}}
  body: none
  auth: none
}

tests {
  test("public can get category", function () {
    expect(res.status).to.equal(200);
    expect(res.body.data.id).to.equal(bru.getVar("createdCategoryId"));
  });
}
