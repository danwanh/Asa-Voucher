meta {
  name: 02 Clear Cart
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/cart/items
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{buyerToken}}
}

tests {
  test("buyer clears cart", function () {
    expect(res.status).to.equal(204);
  });
}
