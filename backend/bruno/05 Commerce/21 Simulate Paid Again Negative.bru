meta {
  name: 21 Simulate Paid Again Negative
  type: http
  seq: 21
}

patch {
  url: {{baseUrl}}/payments/{{seedPaymentId}}/simulate-success
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{buyerToken}}
}

tests {
  test("paid payment cannot issue vouchers again", function () {
    expect(res.status).to.equal(409);
  });
}
