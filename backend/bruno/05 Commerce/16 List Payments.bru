meta {
  name: 16 List Payments
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/orders/{{pendingOrderId}}/payments
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{buyerToken}}
}

tests {
  test("buyer lists order payments", function () {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
