meta {
  name: 08 List Orders
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/orders
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{buyerToken}}
}

tests {
  test("buyer lists orders", function () {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
