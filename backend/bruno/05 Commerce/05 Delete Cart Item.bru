meta {
  name: 05 Delete Cart Item
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/cart/items/{{createdCartItemId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{buyerToken}}
}

tests {
  test("buyer deletes cart item", function () {
    expect(res.status).to.equal(204);
  });
}
