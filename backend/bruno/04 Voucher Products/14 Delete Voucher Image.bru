meta {
  name: 14 Delete Voucher Image
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/voucher-product-images/{{createdVoucherImageId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{partnerToken}}
}

tests {
  test("owner deletes voucher image", function () {
    expect(res.status).to.equal(204);
  });
}
