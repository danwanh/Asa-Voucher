meta {
  name: 09 List Voucher Images
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/voucher-products/{{createdVoucherProductId}}/images
  body: none
  auth: none
}

tests {
  test("public lists voucher images", function () {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
