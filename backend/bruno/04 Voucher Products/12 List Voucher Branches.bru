meta {
  name: 12 List Voucher Branches
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/voucher-products/{{createdVoucherProductId}}/branches
  body: none
  auth: none
}

tests {
  test("public lists voucher branches", function () {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
