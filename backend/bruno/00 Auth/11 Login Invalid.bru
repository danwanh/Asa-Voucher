meta {
  name: 11 Login Invalid
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "buyer@asa.test",
    "password": "wrong-password"
  }
}

tests {
  test("invalid login returns 401", function () {
    expect(res.status).to.equal(401);
  });
}
