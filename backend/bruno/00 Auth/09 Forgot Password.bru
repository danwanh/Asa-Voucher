meta {
  name: 09 Forgot Password
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/auth/forgot-password
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "buyer@asa.test"
  }
}

tests {
  test("forgot password request accepted", function () {
    expect(res.status).to.equal(200);
  });
}
